<h1>All Kittens</h1>
<%= link_to "New Kitten", new_kitten_path %>
<% @kittens.each do |kitten| %>
  <p><%= link_to kitten.name, kitten %></p>
  <%= link_to "Edit", edit_kitten_path(kitten) %>
  <%= button_to "Delete", kitten, method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>
